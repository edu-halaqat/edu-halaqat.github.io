name: Archived Tallam live-verification workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Live verification is handled by public-verify-tallam.yml."