name: Archived Tallam publication workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publication is handled by Deploy GitHub Pages."