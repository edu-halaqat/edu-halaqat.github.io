name: Archived Tallam final-deployment workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The Tallam portal is deployed by Deploy GitHub Pages."